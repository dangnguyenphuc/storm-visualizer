varying float v_value;

void main() {
        float normalized_value = (v_value + 20.0) / (65.0 + 20.0);
        vec3 colors;
        colors[0] = 0.3 + normalized_value;
        colors[1] = normalized_value;  
        colors[2] = 0.8 - normalized_value;
        gl_FragColor = vec4(colors, 1.0);
}
